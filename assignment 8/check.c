quicksort(arr,lb,ub){
    part=partition(arr,lb,ub);
    quick sort(arr,lb,part-1);
    quicksort(arr,part+1,ub);
}
partition(arr,;b,ub){
    pivot=arr[lb];
    i=ub;
    for(j=lb,j<ub,j+){
        if(arr[j]>pivot){
            i++;
            swap(arr[j],arr[i];)
        }
    }
    swap(arr[i+1,arr[i]]);
    return i+1;
}

nerge(arr,lb,mid,ub){
    i=0;k=0;j=mid+1;
    while(i<=mid && j<=ub){
        if(arr[i]<)
    }
    if(i>mid){
        b[k]=arr[j];
        j
    }
}